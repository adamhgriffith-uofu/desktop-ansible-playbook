---

all:
  children:
    group1:
      hosts:
        desktop:
          ansible_host: '<host>'
  vars:
    ansible_user: '<local_user>'
    ansible_password: '<local_user_password>'
    ansible_sudo_pass: '<local_user_sudo_password>'